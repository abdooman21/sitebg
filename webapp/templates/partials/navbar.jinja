<nav class="navbar navbar-expand-lg navbar-dark bg-primary">
    <div class="container-fluid">
        <a class="navbar-brand" href="{{ url_for('main.home') }}">مدونتي</a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarNav">
            <ul class="navbar-nav me-auto">
                <li class="nav-item">
                    <a class="nav-link {{ 'active' if page == 'home' }}" href="{{ url_for('main.home') }}">الرئيسية</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link {{ 'active' if page == 'about' }}" href="{{ url_for('main.about') }}">من نحن</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link {{ 'active' if page == 'contact' }}" href="{{ url_for('main.contact') }}">اتصل بنا</a>
                </li>
            </ul>
        </div>
    </div>
</nav>